# GIT LOGS

Mobile app to visualize the commits of any repository.

To run this app you'll need expo-cli globally installed.

### Use:

```npm install --global expo-cli``` or ```yarn global add expo-cli```

### Get it running with yarn:

1. yarn
2. yarn start

### Get it running with npm

1. npm install
2. npm start

